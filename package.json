{
  "name": "santos-tech-infra",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun --env-file=.env --watch apps/api/src/app.ts",
    "build": "bun run --filter '*' build"
  }
}
