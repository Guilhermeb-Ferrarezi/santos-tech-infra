{
  "name": "santos-tech-infra",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "docker compose -f infra/docker-compose.yml up --build",
    "dev:auth": "cd apps/api-go && go run .",
    "build": "bun run --filter '*' build"
  }
}
