version: "2"
sql:
  - engine: "postgresql"
    queries: "db/query/"
    schema: "db/schema.sql"
    gen:
      go:
        package: "db"
        out: "db/"
        sql_package: "pgx/v5"
        emit_pointers_for_null_types: true
        emit_sql_as_comment: false
